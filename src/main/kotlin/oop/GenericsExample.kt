package oop

/*
Generics - allows us to define a specification of class or method that can be used with any data type.
Classes can have type parameters just like in java.

Type Variance - basically the relation between generic types that share the same base type but have different
type signatures


 */

// covariance - only output is allowed

// contravariance - only input is allowed

// invariance - input and output is allowed; the general <T> parameter